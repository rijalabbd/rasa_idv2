// Route constants for react-router-dom
// Use URL paths for navigation
export const ROUTES = {
  HOME: '/',
  ANALYZE: '/analyze',
  SEARCH: '/search',
};

// List of valid route paths for validation (if needed)
export const VALID_ROUTES = Object.values(ROUTES);

// Default route for fallback
export const DEFAULT_ROUTE = ROUTES.HOME;

